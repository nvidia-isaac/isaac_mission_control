apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mission-control.fullname" . }}-test-connection"
  labels:
    {{- include "mission-control.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mission-control.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
